using UnityEngine;

public class StatusManager : MonoBehaviour
{
    // 訪問者の恩恵によって増えたステータスをまとめる

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
